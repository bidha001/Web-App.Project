package no.ntnu.webapp.models;

/**
 * Enum representing different user roles in the application.
 * The roles are used to define the access level of users in the system.
 */
public enum CourseLevel {
    BEGINNER,
    INTERMEDIATE,
    EXPERT
}