package no.ntnu.webapp.models;

/**
 * Enum representing the visibility status of a course.
 * This determines whether a course is visible to regular users.
 */
public enum CourseStatus {
    ACTIVE,   // Course is visible to users
    INACTIVE  // Course is hidden from users
}
